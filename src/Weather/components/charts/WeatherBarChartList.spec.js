import React from 'react';
import WeatherBarChartList from './WeatherBarChartList';

describe('WeatherBarChart', () => {
  describe('Layout', () => {
    it('should pass', () => {
      expect(1).toBe(1);
    });
  });
});
