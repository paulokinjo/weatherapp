import React from 'react';
import WeatherBarChart from './WeatherBarChart';

describe('WeatherBarChart', () => {
  describe('Layout', () => {
    it('should pass', () => {
      expect(1).toBe(1);
    });
  });
});
