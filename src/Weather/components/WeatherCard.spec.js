import React from 'react';
import WeatherCard from './WeatherCard';

describe('WeatherCard', () => {
  describe('Layout', () => {
    it('should pass', () => {
      expect(2).toBe(2);
    });
  });
});
